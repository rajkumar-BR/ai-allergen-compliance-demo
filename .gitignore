app/.venv/
app/__pycache__/
app/**/__pycache__/
*.pyc

.terraform/
**/.terraform/
.terraform.lock.hcl
**/.terraform.lock.hcl
# Terraform state can contain plaintext resource attributes (never commit it,
# anywhere in this repo - the main stack, terraform/bootstrap/, or any future
# module). Matched recursively, not just at terraform/'s own top level.
*.tfstate
*.tfstate.*
**/*.tfstate
**/*.tfstate.*
terraform/terraform.tfvars
**/tfplan
**/*.tfplan
terraform/build/

/tmp/allergen_local_db.json
/tmp/allergen_uploads/


build/layer/
build/**/.hypothesis/
build/**/.pytest_cache/
build/**/__pycache__/
